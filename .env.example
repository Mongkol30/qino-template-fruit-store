# ============================================
# Environment Configuration Template
# ============================================
# SECURITY: Do NOT commit actual .env files to git!
# Only .env.example should be in version control.
#
# Setup Instructions:
# 1. Copy this file to create your environment config:
#    cp .env.example .env.local          # Local development
#    cp .env.example .env.development    # Development server
#    cp .env.example .env.staging        # Staging environment
#    cp .env.example .env.sit            # System Integration Testing
#    cp .env.example .env.uat            # User Acceptance Testing
#    cp .env.example .env.production     # Production environment
#
# 2. Update the values in your new .env file
# 3. Never commit .env files with real credentials!
# ============================================

# Environment: local | development | staging | sit | uat | production
VITE_ENVIRONMENT=development

# API Configuration
# Replace with your actual API URL for each environment
VITE_API_URL=http://localhost:3000/api
VITE_API_TIMEOUT=30000

# App Info
VITE_APP_NAME=Qino App
VITE_APP_VERSION=1.0.0

# ============================================
# Example values per environment:
# ============================================
# Local:      VITE_API_URL=http://localhost:3000/api
# Development:VITE_API_URL=https://api-dev.example.com/api
# Staging:    VITE_API_URL=https://api-staging.example.com/api
# SIT:        VITE_API_URL=https://api-sit.example.com/api
# UAT:        VITE_API_URL=https://api-uat.example.com/api
# Production: VITE_API_URL=https://api.example.com/api
# ============================================
